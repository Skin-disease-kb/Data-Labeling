# qwen-derm 依赖配置 - 完整版本（推理 + 训练）
# 适用于：运行 annotate_skin_disease.py + Qwen3_VL_(8B)_Vision.ipynb 微调训练
# 安装命令：pip install -r requirements-full.txt

# ============== 核心依赖（必须）==============
# Unsloth 会自动安装兼容的 torch、transformers 等依赖
unsloth>=2025.1.0

# 固定 transformers 版本以兼容 Unsloth 和 TRL
# Unsloth 要求 <=4.56.2，TRL 0.22.2 要求 >=4.55.0
# 4.56.2 同时满足两者
transformers==4.56.2

# 图像处理
Pillow>=9.0.0

# ============== 训练依赖（Notebook 需要）==============
# TRL 用于 SFTTrainer 训练
# 版本 0.22.2 与 transformers 4.56.2 兼容
trl==0.22.2

# 数据集处理
datasets>=2.14.0

# ============== 辅助依赖 ==============
# 加速模型下载
huggingface-hub>=0.23.0

# TRL 的 CLI 依赖（可选但推荐）
tyro>=0.5.7
rich>=13.0.0

# ============== 版本兼容性说明 ==============
# 
# ✅ 已验证的版本组合：
# - transformers==4.56.2
# - trl==0.22.2  
# - unsloth>=2025.1.0
#
# 兼容性矩阵：
# ┌─────────────────┬─────────────┬─────────────┐
# │ 包              │ 要求版本    │ 实际版本    │
# ├─────────────────┼─────────────┼─────────────┤
# │ unsloth         │ <=4.56.2    │ 4.56.2 ✅   │
# │ trl 0.22.2      │ >=4.55.0    │ 4.56.2 ✅   │
# └─────────────────┴─────────────┴─────────────┘
#
# Python 要求: 3.8 - 3.11
# CUDA: 11.8 或 12.x
# 显存: 建议 8GB+（4-bit 量化）
