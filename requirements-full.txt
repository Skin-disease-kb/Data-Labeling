# qwen-derm LoRA training requirements
#
# Scope:
# - train_qwen3_vl_lora_cls.py
# - annotate_skin_disease.py
#
# Recommended environment:
# - Python 3.10
# - Linux server + NVIDIA GPU + CUDA
# - Fresh conda or venv environment
#
# Install order:
# 1. Upgrade packaging tools:
#    python -m pip install --upgrade pip setuptools wheel
# 2. Install the CUDA-matched PyTorch build for the target server:
#    Example (CUDA 12.6):
#    python -m pip install --index-url https://download.pytorch.org/whl/cu126 torch torchvision torchaudio
#    Use the server's actual CUDA / driver combination from pytorch.org if different.
# 3. Install project dependencies:
#    python -m pip install -r requirements-full.txt
#
# Notes:
# - Do not pin torch / torchvision / torchaudio / triton / xformers / bitsandbytes here.
#   Those packages are tightly coupled to the server OS, CUDA runtime, driver, and GPU architecture.
# - If you prefer Unsloth's official CUDA-specific installer, install that first, then run this file.

# Core training stack
unsloth>=2026.2.1
transformers==4.57.6
trl==0.22.2
accelerate>=1.4.0,<2

# Dataset / model IO
datasets>=2.18.0
huggingface-hub>=0.23.0
safetensors>=0.4.3

# Runtime dependencies used directly by the project
numpy>=1.26.0,<3
Pillow>=10.0.0
scikit-learn>=1.4.0
psutil>=5.9.0

# CLI / logging helpers
rich>=13.0.0
tyro>=0.5.7
